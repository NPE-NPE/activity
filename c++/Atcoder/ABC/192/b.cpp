#include <bits/stdc++.h>
using namespace std;
 
int main() {
  string S;
  cin >> S;
  
  
  //奇数番目
  for (int i = 0; i < S.size; i += 2) {
    if (islower(S[i])) cout << 100 - (X % 100) << endl;
    else cout << 100 << endl;

    
  }//if (islower(s))
