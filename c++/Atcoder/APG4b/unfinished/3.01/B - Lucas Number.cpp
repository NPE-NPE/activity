#include <bits/stdc++.h>
using namespace std;
 
int main() {
  int N;
  cin >> N;
  
  vector<int64_t> L(N);
  
  for (int i = 2; i < N; i++) {
    L.at()
    L.at(i - 1) + L.at(i - 2)
    
  
  cout << count << endl;
  
}
