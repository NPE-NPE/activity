#include <bits/stdc++.h>
using namespace std;
 
int main() {
  int N;
  cin >> N;
  
  vector<int> a(N);
  vector<int> b(N);
  
  for(int i = 0; i < N; i++) cin >> a.at(i) >> b.at(i);
  
  pair<int, int> p();
  
  sort(b.begin(), b.end());
  
  cout << p.first << " " << p.second << endl;
  
}
